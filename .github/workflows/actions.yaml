name: default
run-name: default
on: [push]
jobs:
  build:
    runs-on: ubuntu-22.04
    container: alpine:3.16
    steps:
      - uses: actions/checkout@v3
      - run: ls -al
      - run: bin/docker-build.sh
